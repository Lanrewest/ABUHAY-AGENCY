# Real Estate Backend

This is a minimal Express backend scaffold for the MERN real estate platform.

Run:

```
cd backend
npm install
npm run dev
```

Configure environment variables using `.env.example`.
